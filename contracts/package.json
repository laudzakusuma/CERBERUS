{
  "name": "cerberus-contracts",
  "version": "1.0.0",
  "description": "Cerberus Advanced Threat Detection Smart Contracts",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network u2u_testnet",
    "test": "hardhat test",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@types/node": "^18.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.26.3",
    "typescript": "^4.9.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
