[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]
nixPkgs = ["pkgs.gcc", "pkgs.pkg-config"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python advanced_ai_sentinel.py"
